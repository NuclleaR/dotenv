# dnf
alias dn='sudo dnf'
# dnf refresh metadata and upgrade everything
alias dup='sudo dnf makecache && sudo dnf upgrade -y'
# dnf upgrade
alias ddup='sudo dnf upgrade -y'
# dnf search
alias dse='dnf search'
alias dif='dnf info'
# which package provides a file/command
alias dwp='dnf provides'
# dnf install
alias din='sudo dnf install -y'
alias drm='sudo dnf remove'
